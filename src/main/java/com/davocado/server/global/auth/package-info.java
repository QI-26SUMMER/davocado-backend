/**
 * Auth infrastructure: JWT utilities (issue/verify) and the authentication filter.
 *
 * <p>Skeleton only for now — concrete JWT logic and the servlet filter are implemented in a
 * later step (see TODOs in {@code global.config.SecurityConfig}).
 */
package com.davocado.server.global.auth;
