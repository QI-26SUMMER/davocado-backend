/**
 * Auth infrastructure: JWT utilities (issue/verify), the authentication filter, the entry point,
 * and the {@code @CurrentUserId} argument resolver. Wired into the filter chain in
 * {@code global.config.SecurityConfig}.
 */
package com.davocado.server.global.auth;
