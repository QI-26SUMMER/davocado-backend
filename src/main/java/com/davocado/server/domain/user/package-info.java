/**
 * User domain: membership and authentication (self-signup with login id / password + JWT).
 *
 * <p>Sub-packages follow the convention: {@code controller / service / repository / entity / dto}.
 * Entities and endpoints are added in a later step.
 */
package com.davocado.server.domain.user;
