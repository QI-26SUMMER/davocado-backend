/**
 * Scan domain: a single ripeness scan (photo + AI prediction) as one flat record.
 *
 * <p>Sub-packages follow the convention: {@code controller / service / repository / entity / dto}.
 * Only entities exist so far; controller/service/repository/dto are added in a later step.
 */
package com.davocado.server.domain.scan;
